% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_missingness}
\alias{simulate_missingness}
\title{Simulate Missingness in Predictor Variables}
\usage{
simulate_missingness(
  data,
  predictors,
  regime = c("MCAR", "MAR", "MNAR"),
  pi_base = 0.8,
  mar_drivers = NULL,
  mar_weights = NULL,
  mnar_omega = 0,
  outcome_missing = FALSE,
  outcome_col = NULL
)
}
\arguments{
\item{data}{Data frame containing simulated data at governorate-month level}

\item{predictors}{Character vector of predictor column names to apply missingness to}

\item{regime}{Missingness regime: "MCAR", "MAR", or "MNAR"}

\item{pi_base}{Baseline observation probability (for MCAR or intercept)}

\item{mar_drivers}{Character vector of column names driving MAR missingness (e.g., conflict indicators)}

\item{mar_weights}{Named numeric vector of weights for MAR drivers on logit scale}

\item{mnar_omega}{MNAR sensitivity parameter: weight on the (unobserved) value itself}

\item{outcome_missing}{Logical: also apply missingness to outcome variable? Default FALSE.}

\item{outcome_col}{Name of outcome column if outcome_missing = TRUE}
}
\value{
A list containing:
\itemize{
\item data: Original data with values set to NA where not observed
\item observed_indicators: Data frame of observation indicators S_itk (1=observed, 0=missing)
\item missingness_params: List of parameters used
}
}
\description{
Generates observation indicators S_itk ~ Bernoulli(pi_itk) for each predictor column k,
where logit(pi_itk) depends on the specified missingness regime.
}
\details{
Three missingness regimes:
(a) MCAR: S_itk ~ Bernoulli(pi_base) independently
(b) MAR-access: logit(pi_itk) = alpha + w_A\emph{A_it + w_D}D_it + ...
(c) MNAR-severity: logit(pi_itk) = alpha + w_A\emph{A_it + omega}X_itk (depends on unobserved value)
}
\examples{
# Apply MAR missingness driven by conflict intensity
result <- simulate_missingness(
  data = sim_data,
  predictors = c("Nutritional status", "Burden of endemic infectious diseases"),
  regime = "MAR",
  pi_base = 0.7,
  mar_drivers = c("Exposure to armed attacks or mechanical force of nature"),
  mar_weights = c("Exposure to armed attacks or mechanical force of nature" = -0.5)
)
}
