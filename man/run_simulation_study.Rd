% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study.R
\name{run_simulation_study}
\alias{run_simulation_study}
\title{Run Simulation Study for Mortality Estimation}
\usage{
run_simulation_study(
  dag,
  scenarios,
  models = NULL,
  n_replicates = 100,
  mortality_node = "Population mortality",
  predictors = NULL,
  mar_drivers = NULL,
  seed = NULL,
  chains = 2,
  iter = 1000,
  cores = 1,
  verbose = TRUE
)
}
\arguments{
\item{dag}{A nurah_dag object defining the causal structure}

\item{scenarios}{Data frame defining simulation scenarios. Each row is a scenario with columns:
\itemize{
\item scenario_id: Unique identifier
\item n_govs: Number of governorates (spatial units)
\item n_months: Number of months (time periods)
\item initial_pop: Initial population per governorate
\item noise_level: Noise scaling factor (0-1)
\item missingness_regime: "MCAR", "MAR", or "MNAR"
\item pi_base: Baseline observation probability
\item mnar_omega: MNAR sensitivity parameter (if regime = "MNAR")
\item Any additional columns for scenario-specific parameters
}}

\item{models}{Named list of model specifications to fit. Each element should be a list with:
\itemize{
\item formula: Model formula (or NULL to use DAG-derived formula)
\item family: brms family (default: negbinomial())
\item complete_case: Whether to use complete case analysis (default: FALSE)
\item missing_method: "ipw" or "imputation" (if complete_case = FALSE)
Default models include "complete_case", "ipw", and "full_obs" (oracle with no missingness)
}}

\item{n_replicates}{Number of simulation replicates per scenario (default: 100)}

\item{mortality_node}{Name of the mortality outcome node in the DAG}

\item{predictors}{Character vector of predictor variables to include in missingness}

\item{mar_drivers}{Character vector of variables driving MAR missingness}

\item{seed}{Random seed for reproducibility}

\item{chains}{Number of MCMC chains per model fit}

\item{iter}{Number of MCMC iterations per chain}

\item{cores}{Number of CPU cores to use}

\item{verbose}{Whether to print progress messages}
}
\value{
A data frame with one row per scenario x model x replicate containing:
\itemize{
\item scenario_id, model_id, replicate
\item true_total_deaths: True total deaths from simulation
\item est_total_deaths: Estimated total deaths from model
\item bias: Absolute bias (estimated - true)
\item rel_bias: Relative bias (bias / true)
\item rmse: Root mean squared error
\item coverage: Whether 95\% CI contains true value
\item Additional governorate-level metrics
}
}
\description{
Main function for running a simulation study to evaluate mortality estimators
under various scenarios (effect strengths, noise levels, missingness regimes).
}
\examples{
\dontrun{
# Define scenarios
scenarios <- expand.grid(
  n_govs = 10,
  n_months = 12,
  initial_pop = 100000,
  noise_level = c(0.5, 1.0),
  missingness_regime = c("MCAR", "MAR", "MNAR"),
  pi_base = c(0.5, 0.8),
  mnar_omega = c(0, -0.5)
)
scenarios$scenario_id <- seq_len(nrow(scenarios))

# Run study
results <- run_simulation_study(
  dag = checchi_2017_dag(dummy_checchi_2017_parameters()),
  scenarios = scenarios,
  n_replicates = 10,
  mortality_node = "Population mortality"
)
}

}
